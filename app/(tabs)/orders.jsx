import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Orders = () => {
  return (
    <View style={{ justifyContent: "center", alignItems: "center", flex: 1 }}>
      <Text>Tab [Orders]</Text>
    </View>
  );
};

export default Orders;

const styles = StyleSheet.create({});
